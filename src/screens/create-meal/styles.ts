import styled from "styled-components/native";

export const IsInDietLabel = styled.Text(({ theme }) => ({
  fontFamily: theme.FONT_FAMILIES.BOLD,
  color: theme.COLORS.BASE.GRAY_200,
  fontSize: theme.SIZES.SM,
}));
